services:
  - type: web
    name: vet-portal
    runtime: static
    buildCommand: echo "Static site"
    staticPublishPath: ./public
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
